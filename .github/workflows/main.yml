name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main # o il nome del branch che stai usando

permissions:
  contents: write
  issues: write
  packages: write
  pull-requests: write
  repository-projects: write
  discussions: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js 20.x
        uses: actions/setup-node@v1
        with:
          node-version: "20.x" # Assicurati di usare la versione di Node.js che hai localmente
      - run: npm install
      - run: npm run build
      - run: npm run export
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out
